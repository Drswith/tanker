import {
  request,
} from './request'

export default {
  // 使用规则
  // usageExamples(config) {
  // 	return request({
  // 		url: `/api/url`,
  // 		method: "GET", //GET/POST/PUT/DELETE
  // 		data: config,
  // 		contentType: 'application/x-www-form-urlencoded', //默认为'application/json' 可自由定义
  // 		notShowLoading: true,  //是否隐藏加载中弹框
  // 	})
  // }
  // usageLogin(config) {
  //   return request({
  //     url: `/api/auth/login`,
  //     method: 'POSt', // GET/POST/PUT/DELETE
  //     data: config,
  //     contentType: 'application/x-www-form-urlencoded', // 默认为'application/json' 可自由定义
  //     notShowLoading: true, // 是否隐藏加载中弹框
  //   })
  // },

  // usageMapCoordinates(config) {
  // 	return request({
  // 		url: `/api/order/map_coordinates`,
  // 		method: "GET", //GET/POST/PUT/DELETE
  // 		data: config,
  // 		contentType: 'application/x-www-form-urlencoded', //默认为'application/json' 可自由定义
  // 		notShowLoading: true,
  // 	})
  // },

  // usageOrderList(config) {
  //   return request({
  //     url: `/api/order/search`,
  //     method: 'GET', // GET/POST/PUT/DELETE
  //     data: config,
  //     contentType: 'application/x-www-form-urlencoded', // 默认为'application/json' 可自由定义
  //     notShowLoading: true,
  //   })
  // },

  // usageCurrentOrder(config) {
  // 	return request({
  // 		url: `/api/order/current_order`,
  // 		method: "GET", //GET/POST/PUT/DELETE
  // 		data: config,
  // 		contentType: 'application/x-www-form-urlencoded', //默认为'application/json' 可自由定义
  // 		notShowLoading: true,
  // 	})
  // },

}
